
/**
 * Minimal PDF.js worker that properly redirects to the CDN version
 * This ensures reliable PDF processing without bundling the entire worker
 */

// Import the actual PDF.js worker from CDN
importScripts('https://cdn.jsdelivr.net/npm/pdfjs-dist@5.2.133/build/pdf.worker.min.js');

console.log('PDF.js worker loaded from CDN successfully');
